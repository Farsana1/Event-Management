import React from 'react';
import './Logo.css'; // Optional CSS for logo styling

const Logo = () => {
  return (
    <h1 className="logo-title">
      Event<span>ify</span>
    </h1>
  );
};

export default Logo;
